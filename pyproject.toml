[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "katalog"
version = "0.1.0"
description = "Projeto Katalog com melhorias DDD"
authors = ["Equipe DDD"]
